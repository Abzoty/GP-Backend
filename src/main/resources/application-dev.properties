# Local Server 
server.port=8080 

# Disable open-in-view to prevent lazy DB queries from firing during HTTP response rendering
spring.jpa.open-in-view=false

# Local Security Testing
spring.security.user.name=admin
spring.security.user.password=0123

# Local DataSource — MS SQL Server (Windows Authentication)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=GP_db;encrypt=false;integratedSecurity=true
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Local JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# # Local ML Service 
# ml.service.base-url=http://localhost:5001

# JWT Secret Key (for development only, use a secure key in production)
jwt.secret=mySuperSecretJwtKeyThatIsAtLeast32CharactersLong!
jwt.expiration-ms=900
jwt.refresh-expiration-ms=604800

logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

# # Email (for notifications) 
spring.mail.host=covalent.platform@gmail.com
spring.mail.port=587
spring.mail.username=covalent.platform@gmail.com
spring.mail.password=bkia lkgp nxck rlot
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true